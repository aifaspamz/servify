Servify uses four main data structures: unordered_map, vector, queue, and stack.
The unordered_map stores provider profile details using key-value pairs. The vector
manages multiple providers and services. The queue organizes client bookings in
first-come, first-served order. The stack supports an undo feature by storing recently
deleted services, allowing the most recent deleted service to be restored first.
